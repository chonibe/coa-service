// Note: Mantine doesn't have a built-in chart component
// We'll keep using the existing chart library (recharts) but wrap it with Mantine styling
import { Box } from "@mantine/core"
import {
  Area,
  AreaChart,
  Bar,
  BarChart,
  CartesianGrid,
  Legend,
  Line,
  LineChart,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} from "recharts"

export {
  Area,
  AreaChart,
  Bar,
  BarChart,
  Box,
  CartesianGrid,
  Legend,
  Line,
  LineChart,
  ResponsiveContainer,
  Tooltip,
  XAxis,
  YAxis,
} 